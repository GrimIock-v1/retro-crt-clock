services:
  retro-clock-bridge:
    build: .
    container_name: retro-clock-bridge
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/data
